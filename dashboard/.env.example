# Base URL of the slice gateway's read-only stats API.
# Vite only exposes vars prefixed with VITE_ to the app. Copy this to `.env`
# (kept out of git) to override; the dev default already points at the local
# gateway, so for local dev you usually don't need a .env at all.
VITE_API_BASE=http://localhost:8080/api
