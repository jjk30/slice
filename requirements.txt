fastapi
uvicorn
httpx
asyncpg
redis
langgraph
langchain-core
sentence-transformers
faiss-cpu
numpy
# --- Evaluation (phase 8) ---
# Pinned, unlike the rest of this file: ragas pulls a wide, fast-moving dependency
# tree (its own langchain meta-packages, datasets, pandas), so the exact versions
# that were verified against the installed langchain-core (1.5.5) / langgraph
# (1.2.11) are frozen here. ragas 0.4.3 resolves cleanly against those without
# forcing either up or down (see the phase-8 notes in the README). langchain-anthropic
# backs the RAGAS judge; langsmith (already an indirect langchain dependency) is
# pinned explicitly because phase 8 uses its tracing directly.
ragas==0.4.3
langchain-anthropic==1.5.6
langsmith==0.11.0
# --- Guardrails (phase 9) ---
# Pinned. nemoguardrails is notorious for strict langchain pins, so this version was
# chosen by checking its declared dependencies against the existing pins BEFORE
# installing. 0.23.0 (the latest) declares NO langchain/langchain-core/langchain-community
# constraint at all and no fastapi/starlette/uvicorn core constraint, so it forces none
# of the phase 5-8 pins up or down: langchain 1.3.15, langchain-core 1.5.5,
# langchain-community 0.4.2, langgraph 1.2.11, langchain-anthropic 1.5.6, langsmith
# 0.11.0, and ragas 0.4.3 all stay exactly as they were (verified with a resolver
# dry-run). The one pre-existing package it moves is pandas (3.0.5 -> 2.3.3, its
# `pandas<3` cap) — pandas is transitive, not a named pin, and ragas 0.4.3 requires it
# only under `extra == "all"`, so ragas is unaffected. The rails LLM is wired through the
# already-pinned langchain-anthropic. See the phase-9 notes in the README.
nemoguardrails==0.23.0
pytest
pytest-asyncio
respx
fakeredis
python-dotenv

# --- Auth (phase 12): GitHub device-flow login, slice keys, JWT sessions, the CLI. ---
# PyJWT signs and verifies the dashboard-session JWTs (HS256). typer (over click) is the
# `slice` CLI — login, init, use — installed as a console entry point via pyproject.toml
# (`pip install -e .`). Both are small and pure-Python; neither touches the pinned
# langchain/ragas tree, so they force nothing up or down.
PyJWT
typer
mcp==1.29.0

# --- Monitoring (phase 17): Prometheus metrics. ---
# prometheus-client exposes the gateway's /metrics endpoint (Prometheus text format)
# and holds the slice_* counters/histogram. Pure-Python, no native deps, and it pulls
# in nothing else — so it forces none of the pinned langchain/ragas tree up or down.
prometheus-client
